package tools;

import java.security.NoSuchAlgorithmException;
import java.sql.Connection;
import java.sql.Statement;
import java.util.Random;

public class ConnectionTools {

	public static void removeConnection(String key) {
		// TODO Auto-generated method stub
		
	}

	public static boolean isConnected(String key) {
		// TODO Auto-generated method stub
		return false;
	}

	public static String generateKey(Connection c) {
		Random r = new Random();
		return String.valueOf(r.nextInt(999999999));
	}
	
	public static void insertConnexion(String key, int id_user) {
		String update = "INSERT INTO SESSIONS VALUES(\"" + stringKey+ "\", \"" + login + "\", PASSWORD(\"" + password + "\", \"" + name + "\", \"" + lastName + "\", \"" + email + "\");";
		Statement st = c.createStatement();
		int res = st.executeUpdate(update);
		st.close();
		return res;
		
	}
}
